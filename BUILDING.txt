install check-manifest
then check-manifest  
pip install flake8
- checking formatting
flake8 setup.py tests src |cat -n
- fixing them
autopep8 --in-place --recursive --max-line-length=100 src tests *.py -v -a
# following can help removing unused imports 
autoflake --in-place --remove-unused-variables example.py

then try 
flake8 src *.py tests

isort errors:
isort  --check-only --diff --recursive src tests *.py service/*.py
fixing isort errors: 
isort <filename> 
isort *.py tests src -rc -v

